<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>my first app</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

  <div class="container">
    <h1>Crack the problem</h1>
    <% if(error) { %>
      <p class="error"><%= error %></p>
    <% } else { %>
      <p><%= problem %></p>
      <h2>Your Answer</h2>
      <form action="/check" method="post">
        <input type="hidden" name="instruction" value="<%= problem %>">
        <input type="hidden" name="answer_code" value="<%= answer_code %>">
        <textarea name="user_code"><%= initial_code %></textarea>
        <input class="btn" type="submit" value="Check">
      </form>
    <% } %>
  </div>

</body>

